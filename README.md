Test of compiling beamer presentation with animate package enabled....
======================================================================

Run `make.sh` to coduct a test.
Run `clean.sh` to delete all files that are generated during the test.
